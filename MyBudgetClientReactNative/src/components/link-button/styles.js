import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    button: {
        width: 300,
    },

    buttonText: {
        color: '#337ab7',
        textAlign: 'right',
        paddingHorizontal: 10,
    },
});
